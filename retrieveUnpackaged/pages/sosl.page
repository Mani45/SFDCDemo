<apex:page showHeader="false" controller="aapsosl">
 <apex:form >
   <apex:pageBlock >
    <apex:pagemessages ></apex:pagemessages>

     <apex:pageblocksection title="course Records">
       <apex:pageblocktable value="{!arecs}" var="item">
         <apex:column value="{!item.id}"/>
         <apex:column value="{!item.name}"/>
       </apex:pageblocktable>
     </apex:pageblocksection>
     <apex:pageblocksection title="lead Records">
       <apex:pageblocktable value="{!lrecs}" var="item">
         <apex:column value="{!item.id}"/>
         <apex:column value="{!item.name}"/>
        </apex:pageblocktable>
      </apex:pageblocksection> 
   </apex:pageBlock>
   <apex:commandButton value="Click.." action="{!m1}"/>
 </apex:form>
</apex:page>